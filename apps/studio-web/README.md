# Studio Web

Phase 0 only: shell placeholder.

Phase 4 Simple Mode contract:
upload -> generate -> QA -> live preview -> camera/mic -> export

Do not implement Pro Editor before Phase 1 runtime and Phase 2 rig compiler are stable.
