{
  "name": "@a2d/studio-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json && vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test"
  },
  "dependencies": {
    "@a2d/runtime-api": "workspace:*"
  },
  "devDependencies": {
    "typescript": "5.8.3",
    "vite": "^8.0.0"
  }
}
